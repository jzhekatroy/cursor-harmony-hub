name: Advanced Deploy with Database Migrations

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      skip_migrations:
        description: 'Skip database migrations (dangerous!)'
        required: false
        default: 'false'
        type: boolean

env:
  NODE_VERSION: '18'
  DATABASE_URL: ${{ secrets.DATABASE_URL }}

jobs:
  pre-deploy-checks:
    runs-on: ubuntu-latest
    outputs:
      should_deploy: ${{ steps.check.outputs.should_deploy }}
      
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Type Check
      run: npm run build
        
    - name: Check if deployment is needed
      id: check
      run: |
        # Проверяем, есть ли изменения в схеме базы данных
        if git diff --name-only HEAD~1 HEAD | grep -E "(prisma/schema.prisma|prisma/migrations)" > /dev/null; then
          echo "should_deploy=true" >> $GITHUB_OUTPUT
          echo "📊 Database schema changes detected"
        else
          echo "should_deploy=false" >> $GITHUB_OUTPUT
          echo "📊 No database schema changes"
        fi

  database-migration:
    needs: pre-deploy-checks
    if: needs.pre-deploy-checks.outputs.should_deploy == 'true' && github.event.inputs.skip_migrations != 'true'
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Generate Prisma Client
      run: npx prisma generate
        
    - name: Backup Database (if possible)
      run: |
        echo "💾 Creating database backup..."
        # Здесь можно добавить команды для бэкапа базы данных
        # Например, pg_dump для PostgreSQL
        
    - name: Run Database Migrations
      run: |
        echo "🔄 Running database migrations..."
        npx prisma migrate deploy
        
    - name: Verify Migration Success
      run: |
        echo "✅ Verifying migration success..."
        npx prisma db push --accept-data-loss
        
    - name: Run Database Seed (if needed)
      run: |
        echo "🌱 Seeding database..."
        npx prisma db seed
      continue-on-error: true

  deploy:
    needs: [pre-deploy-checks, database-migration]
    if: always() && (needs.database-migration.result == 'success' || needs.database-migration.result == 'skipped')
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Generate Prisma Client
      run: npx prisma generate
        
    - name: Build Application
      run: npm run build
        
    - name: Deploy to Production
      run: |
        echo "🚀 Deploying to production..."
        # Здесь добавьте команды для деплоя на ваш хостинг
        # Например, для Vercel, Netlify, или собственного сервера
        
    - name: Post-deploy Health Check
      run: |
        echo "🔍 Running post-deploy health check..."
        # Добавьте проверку работоспособности после деплоя
        # Например, проверка API endpoints
        
    - name: Notify Success
      run: |
        echo "✅ Deployment completed successfully!"
        # Здесь можно добавить уведомления (Slack, Discord, email)

  rollback:
    needs: [pre-deploy-checks, database-migration, deploy]
    if: failure() && needs.database-migration.result == 'success'
    runs-on: ubuntu-latest
    
    steps:
    - name: Rollback Database
      run: |
        echo "🔄 Rolling back database changes..."
        # Здесь можно добавить команды для отката миграций
        # Например, npx prisma migrate reset
        
    - name: Notify Failure
      run: |
        echo "❌ Deployment failed and rollback completed"
        # Здесь можно добавить уведомления об ошибке
